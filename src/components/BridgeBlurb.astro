---
const {
  text = "Transforming Spaces Into Experiences",
  maxWidth = "max-w-4xl",
} = Astro.props;
---

<div class="relative z-10">
  <div
    class={`mx-auto ${maxWidth} -mt-16 md:-mt-24 lg:-mt-28 rounded-2xl bg-white/95 px-6 py-8 md:px-10 md:py-12 shadow-[0_8px_30px_rgba(0,0,0,0.12)]`}
  >
    <p class="text-center text-xl md:text-2xl leading-relaxed text-neutral-700">
      {text}
    </p>
  </div>
</div>
